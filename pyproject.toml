[project]
name = "pico-adder-nn-no-training"
version = "0.1.0"
description = "Build AI model without training"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.2",
    "torch>=2.10.0",
]
